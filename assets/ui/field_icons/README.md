# Field icons

Project-authored SVG silhouettes, 512×512 import size, scalable view boxes.
These schematic icons identify gameplay categories/variants; they are not exact
manufacturer product renders. No copied product photographs or logos.
Consumed automatically by ItemCatalog._item() when the SVG name matches the item ID.
